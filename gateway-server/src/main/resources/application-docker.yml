# =============================================================================
# docker profili - konteyner / docker-compose ortamı
# Eureka, compose ağı içinde 'eureka-server' host adıyla çözümlenir.
# =============================================================================
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
