spring:
  cloud:
    config:
      server:
        git:
          clone-on-start: true
          force-pull: true

logging:
  level:
    root: WARN
    com.etiya: INFO
    org.springframework.cloud.config: INFO
